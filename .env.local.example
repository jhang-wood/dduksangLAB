# 떡상연구소 환경변수 템플릿
# 이 파일을 .env.local로 복사하고 실제 값으로 교체하세요

# ===========================================
# Supabase 설정 (필수)
# ===========================================
# Supabase 프로젝트 URL
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url

# Supabase 익명 키 (클라이언트 사이드용)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase 서비스 역할 키 (서버 사이드용)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ===========================================
# 애플리케이션 설정
# ===========================================
# 애플리케이션 URL (프로덕션에서는 https://dduksang.com)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# CRON 시크릿 (API 보안용)
CRON_SECRET=your-secure-cron-secret-key

# ===========================================
# 개발/테스트 설정 (선택사항)
# ===========================================
# 개발 모드에서 항상 Mock 데이터 사용 (true/false)
FORCE_MOCK_MODE=false

# 디버그 로깅 활성화 (true/false)
DEBUG_MODE=false

# ===========================================
# 설정 가이드
# ===========================================
# 1. Supabase 대시보드에서 프로젝트 URL과 API 키를 확인하세요
# 2. 실제 키 값으로 "your-" 접두사가 붙은 플레이스홀더를 교체하세요
# 3. 프로덕션 환경에서는 Vercel 대시보드의 Environment Variables에 설정하세요
# 4. 보안을 위해 이 파일(.env.local)은 절대 Git에 커밋하지 마세요

# 주의: 더미 키(예: your-supabase-anon-key)가 감지되면 자동으로 Mock 모드로 전환됩니다